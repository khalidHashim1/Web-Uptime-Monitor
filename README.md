# Web-Uptime-Monitor
Serverless website uptime monitoring system built with AWS and Terraform. Uses a Python Lambda function to check website availability, response time, and content integrity every 5 minutes, stores logs in DynamoDB, sends alerts via SNS, and displays uptime metrics through an S3-hosted dashboard.
